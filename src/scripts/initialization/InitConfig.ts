export interface InitConfig {
  /**
   * HTTP URL to the log file.
   */
  logFileUrl?:string;

  /**
   * HTTP URL to the rss.
   */
  rssFeedUrl?:string;

  /**
   * HTTP URL to the feed.
   */
  jsonFeedUrl?:string;
}
